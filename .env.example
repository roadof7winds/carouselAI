TELEGRAM_BOT_TOKEN=123456:ABC-your-telegram-bot-token

# Used when running components without Docker; inside docker-compose these are
# set explicitly per service instead (see docker-compose.yml).
CAROUSELAI_DATA_DIR=./data
CAROUSELAI_WEB_HOST=127.0.0.1
CAROUSELAI_WEB_PORT=8000
