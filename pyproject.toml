[project]
name = "carouselai"
version = "0.1.0"
description = "Telegram bot + MCP server for generating and editing Instagram carousel slides"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.6",
    "pillow>=10.3",
    "aiogram>=3.4",
    "python-dotenv>=1.0",
    "mcp>=2.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]

[project.scripts]
carouselai-bot = "carouselai.bot.main:main"
carouselai-mcp = "carouselai.mcp_server.server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/carouselai"]

[tool.pytest.ini_options]
pythonpath = ["src"]
